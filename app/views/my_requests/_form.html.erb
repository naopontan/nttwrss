<%= form_for(@my_request) do |f| %>
  <% if @my_request.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@my_request.errors.count, "error") %> prohibited this my_request from being saved:</h2>

      <ul>
      <% @my_request.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :url %><br />
    <%= f.text_field :url %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
